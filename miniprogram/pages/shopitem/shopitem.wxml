<view wx:if="{{!loading}}" class="page-container">
  <ban-bar />

  <!-- 必填的只有名称和价格，其余一律归到「详细信息（选填）」，
       跟小店设置那张表同一套分节口径 -->
  <view class="section-title">基本信息（必填）</view>
  <input placeholder="商品名称" value="{{name}}" bindinput="onInput" data-field="name" class="card" style="padding: 24rpx;" />
  <input placeholder="价格（美元）" type="digit" value="{{price}}" bindinput="onInput" data-field="price" class="card" style="padding: 24rpx;" />

  <view class="section-title">详细信息（选填）</view>

  <view class="img-row" bindtap="chooseImage">
    <image wx:if="{{image}}" src="{{image}}" mode="aspectFill" class="dish-img" />
    <view wx:else class="dish-img dish-img-placeholder">+</view>
    <view class="text-light img-hint">点击{{image ? '更换' : '上传'}}商品图片</view>
  </view>

  <input placeholder="单位（如 份 / 杯 / 件 / 次 / 小时）" value="{{unit}}" bindinput="onInput" data-field="unit" maxlength="4" class="card" style="padding: 24rpx;" />
  <input wx:if="{{useCategory}}" placeholder="分类（如 主食 / 小食 / 饮品）" value="{{category}}" bindinput="onInput" data-field="category" class="card" style="padding: 24rpx;" />
  <textarea placeholder="商品描述（规格、分量等）" value="{{description}}" bindinput="onInput" data-field="description" class="card" style="padding: 24rpx; width: auto; height: 140rpx;"></textarea>

  <view class="subsection-title">成分与规格说明</view>
  <view class="field-hint">如实说明材质、成分或服务内容。<text class="strong">销售食品的必须标注过敏原</text>（花生、坚果、乳制品、鸡蛋、大豆、小麦、海鲜等），隐瞒或错填的后果由你承担，详见《店长责任告知书》。</view>
  <textarea placeholder="食品如：含花生、乳制品、麸质…；其他商品如：材质、尺寸、服务时长" value="{{allergens}}" bindinput="onInput" data-field="allergens" class="card" style="padding: 24rpx; width: auto; height: 120rpx;"></textarea>

  <view class="card avail-row">
    <view>
      <view>当前上架</view>
      <view class="text-light avail-hint">今日是否售罄</view>
    </view>
    <switch checked="{{available}}" color="#7A0019" bindchange="onAvailableChange" />
  </view>

  <button class="btn-primary" bindtap="submit" loading="{{saving}}" style="margin-top: 32rpx;">保存</button>
</view>
